# Changer TARGET_IP dans real_traffic_demo.py
TARGET_IP = "192.168.X.X"  # IP de son PC

# Lancer la démo
python real_traffic_demo.py --demo full
```

---

**Ce que le jury va voir**
```
Grand écran (PC collègue)          Ton PC (terminal)
──────────────────────────         ──────────────────
Dashboard Streamlit                [10:23:01] 🚨 PortScan     DÉTECTÉ
  🚨 Alertes en temps réel    ←    [10:23:05] 🚨 DDoS         DÉTECTÉ
  📊 Graphiques qui s'updatant     [10:23:12] ✅ BENIGN        SAIN
  🔒 Flux bloqués : 47             [10:23:18] 🚨 Slowloris    DÉTECTÉ
```

---

**Ordre recommandé pour impressionner**
```
1. Montrer le dashboard vide        "Voici notre IDS au repos"
2. Lancer --demo normal             "Trafic normal — aucune alerte"
3. Lancer --demo scan               "PortScan détecté immédiatement"
4. Lancer --demo ddos               "DDoS bloqué en temps réel"
5. Lancer --demo full               "Scénario complet automatique"
```

---

**Avant la démo — checklist**
```
✅ Les deux PCs sur le même réseau WiFi
✅ Ports 8000 et 8501 ouverts sur son PC
✅ FastAPI qui tourne sur son PC
✅ Streamlit projeté sur grand écran
✅ TARGET_IP modifié dans ton fichier
✅ Tester une fois avant le jury


Oui ! C'est même meilleur que real_traffic_demo.py pour ça.
Tu changes juste une ligne dans traffic_simulator.py :
python# Ligne ~25
API_URL = "http://localhost:8000"

# Remplace par l'IP de ton collègue
API_URL = "http://192.168.X.X:8000"
Et sur le PC de ton collègue :
bashuvicorn main:app --host 0.0.0.0 --port 8000 --reload
streamlit run streamlit_app.py
Sur ton PC :
bashpython traffic_simulator.py --chaos --rate 2
